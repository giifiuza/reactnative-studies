import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    
    input:{
        borderWidth: 1,
        borderColor: '#0077B6',
        height: 50,
        marginBottom: 12,
        fontSize: 16,
        color: '#ffffff',
        borderRadius: 20,
        padding: 10

    }
})

export default styles
